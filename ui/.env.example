# The frontend uses same-origin /api requests and requires no runtime
# environment variables. Never put server credentials or private keys here:
# anything exposed to a browser must be treated as public.
